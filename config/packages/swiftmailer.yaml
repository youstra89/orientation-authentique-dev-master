swiftmailer:
    # url: '%env(MAILER_URL)%'
    # spool: { type: 'memory' }
    transport: smtp
    auth_mode: login
    encryption: ssl
    port: 465
    host: smtp.gmail.com
    username: contact.youstra@gmail.com
    password: youstra89
    spool: { type: 'memory' }
